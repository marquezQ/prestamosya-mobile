/**
 * stores/index.ts
 *
 * Punto de entrada para todos los stores de Zustand.
 * Exportar desde aquí para mantener imports limpios:
 *   import { useAuthStore } from "@/stores";
 *
 * NOTA: No se usa middleware de persistencia (persist / createJSONStorage)
 * hasta que se defina el mecanismo de storage (PED-8 / PED-13).
 * Los stores son exclusivamente en memoria por ahora.
 */

// export { useAuthStore } from "./auth.store";   // ← descomentar cuando se cree
// export { useLoanStore } from "./loan.store";   // ← descomentar cuando se cree
